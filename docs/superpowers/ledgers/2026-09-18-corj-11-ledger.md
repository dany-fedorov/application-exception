# SDD ledger — plan: /home/df/wd/personal/application-exception/docs/superpowers/plans/2026-09-18-corj-11-orthogonal-reports.md
Repo under work: /home/df/wd/personal/caught-object-report-json, branch feat/orthogonal-reports, merge base 5ad780e
Owner directive (2026-09-18): execute all with Opus medium, push and merge; "don't ask questions - assume and document".
Executor model: opus for implementers and reviewers, "medium reasoning effort" stated in every prompt. Final adversarial review: session model (Fable).

## Pre-flight scan
| Tasks | Produces vs consumes | Finding |
| --- | --- | --- |
| 1 -> 2 | CorjContext -> CorjReportingError = CorjContext & {error} | consistent |
| 2 -> 3,4 | collecting(), ctx.errors, reportError -> makeJson, build(context) | consistent; finish seals ctx.errors=null, collecting restores |
| 3 -> 4 | makeAsJson(ctx,node,view{lengthLimit}) -> context view at $context | consistent |
| 4 -> 5 | context on root, context_omitted declared -> limiter strips | DEFECT: Task 5 test "a context that fits is never touched, even in a trimmed report" contradicts spec D4 drop order and the task's own implementation |
| 5 -> 6,7 | fixed fields by name in limiter + minimal report -> occurrence_id, fingerprint | consistent; floor measured 487/512 |
| 6 -> 7 | access(inspection), readEntry, validateSourceEntry, FINGERPRINT_PATTERN | consistent |
| 7 self | fallback "root row all empty" vs test "thrown primitives do not all share one fingerprint" | DEFECT: a thrown string has constructor_name "String" (verified), so the row is never empty and the test would fail |
| 6,7 -> 8 | defaults null -> on; LEGACY bag | consistent |
| 8 -> 9 | schemas for the six root fields | consistent |
| each task | tests vs code within the task | Tasks 1,2,3,4,6,8,9,10 agree with themselves (2 and 5 tests were fixed during plan self-review) |

Ruling: Task 5 test rewritten to follow spec D4 (context is dropped whole before any content is trimmed, however small) — the spec is the authority and it is appex's behaviour today — if wrong: a tiny context survives less often than a caller expects; one-line limiter change later.
Ruling: fingerprint fallback [typeof, as_string] applies when the root has no string stack OR its row is empty (fingerprintOf gains forceFallback) — needed for the spec's own goal that thrown primitives do not share a fingerprint — if wrong: fingerprints of stackless values include their string form, which can over-split plain-object throws with varying toString.
Both fixes committed to the plan and spec in appex 025b3fd.

Task 0: complete (branch cut; baseline 19 suites, 1204 tests, 100% coverage)
Ruling: the lint gate is `npx eslint src tests --ext .ts` and `npx prettier --check` on changed files — repo-wide `eslint .` / `prettier --check .` fail on main already (examples, jest.config.ts, docs JSON), outside this plan's scope — if wrong: pre-existing lint debt stays as it was.
Task 1: reviewer ⚠️ resolved by controller: `npm run test-consumers` run at 4f5f060, all six checks PASS (types included), so the index.ts<->redaction.ts type-only cycle survives the built declaration layout.
Task 1: Ruling: reviewer Important "commit footer under-reports the break (stage unions widened; CorjRedactContext.key narrowed from string to CorjReportKey)" — plan-mandated footer; no history rewrite; both items are added to Task 9's "Upgrading from v10" table and to Task 9's BREAKING CHANGE footer, which is what semantic-release publishes — if wrong: the generated changelog entry for commit 4f5f060 alone stays incomplete.
Task 1: minor (deferred): tests/context-types.test.ts assertions are type-level only (tautological at runtime); brief-verbatim.
Task 1: minor (deferred): src/redaction.ts imports types from the index barrel (type-only cycle).
Task 1: minor (deferred): README.md:1451,1567 and API-REVIEW.md still describe removed aliases — Task 9 README rewrite must cover it.
Task 1: complete (commits 5ad780e..4f5f060, review clean after ruling)
Ruling: Task 2 fuzz seeds (budgets 256/300 fall to the minimal report because the limiter cannot drop reporting_errors until Task 5; a plan-ordering defect of mine) — Option A: tests/fuzz.test.ts accepts the structural root id for a minimal-fallback report — if wrong: the fuzz id assertion is slightly weaker for minimal reports only.
Task 2: reviewer ⚠️ resolved: reports carry reporting_errors while still labelled corj/v0.13 until Task 9 — nothing ships between tasks (single branch, unmerged); Task 9 adds the v0.14 schemas.
Task 2: fix round 1/5 (2 addressed, 0 open — fuzz minimal predicate tightened; onError gets a copy of the record; commits cac4cf9..15fff71)
Task 2: Ruling: reviewer Minor "handler receives the stored record object" fixed in round 1 instead of deferred — one line, protects report integrity — if wrong: one extra object spread per failure.
Task 2: minor (deferred): two existing tests assert reporting_errors self-referentially from the handler's calls; literal content is pinned in tests/reporting-errors.test.ts.
Task 2: complete (commits 4f5f060..15fff71, review clean after round 1)
Task 3: dispatched, BASE 15fff71
Task 3: implemented b85178b, review dispatched; Task 4 dispatched (BASE b85178b) — pipelining: next implementer starts while the previous task is reviewed; fixes queue behind the running implementer
Task 3: review: spec ✅, quality approved, 1 Important (unbounded serializer's circular/no-invoke safety untested) — fix queued behind Task 4's implementer (same working tree)
Task 3: minor (deferred): test name "built once and reused" overclaims; StringifyConfig mirror needs a comment; dropped `#apply` symbol assertion; redact-stage record path is the replacement so the failure location is lost (fail-closed by design, final review to weigh); numeric maxSize with null maxReportSize unpinned; `errors as` cast in makeJson; view drops `format`.
Task 3: fix round 1/5 dispatched; commit 4bb4cb3. Task 5 dispatched (BASE 4bb4cb3)
Task 3: fix round 1/5 (1 addressed + 1 minor, 0 open; commits 528895a..4bb4cb3)
Task 3: complete (commits 15fff71..b85178b + 4bb4cb3, review clean after round 1)
Task 4: review: spec ✅, quality approved, 1 Important (three copies of the size validator with drifting messages) — fix queued behind Task 5's implementer; minors 1,2,5 ride along (null/array call input tests, read call.context once, object-shape children have no context)
Task 4: minor (deferred): duplicated view-node literal (viewNode factory); reportSizeUnit doc does not mention maxContextSize.
Task 5: implemented 907fd49 (DONE_WITH_CONCERNS: one regression test lost its $schema-drop meaning at the new floor; example-10 and README still say 256 -> carried into Task 8 and Task 9 briefs). Task 4 fix round 1 dispatched.
Task 4: fix commit cfe25c5; Task 6 dispatched (BASE cfe25c5)
Task 4: fix round 1/5 (4 addressed, 0 open; commits 907fd49..cfe25c5)
Task 4: complete (commits b85178b..528895a + cfe25c5, review clean after round 1)
Task 5: review: spec ✅, quality NEEDS FIXES, 1 Important (review-regressions "$schema dropped before content" assertion inverted; a payload with omitExpectedValues:false at budgets [512,560] restores it) — fix queued behind Task 6's implementer; minors 2 (spend the duplicate fuzz 512 slot on 700) and 5 (array-shape case in limiter-order) ride along
Task 5: minor (deferred): report-size-invariants budgets list changed its 512 entry to 600; floor message hardcodes 512; hardcoded 'corj/v0.13' in new expectations (Task 9 bump will touch them).
Task 6: implemented bec632b; review dispatched. Task 5 fix round 1 dispatched. Note: corj v0.13 schemas have no additionalProperties:false, so new root fields validate until Task 9 adds v0.14.
Task 5: fix commit 83169c0 (tests only); re-review dispatched
Task 6: review: spec ✅, quality approved, no Critical/Important
Task 6: Ruling: reviewer ⚠️ "minimal array root puts occurrence_id after id/path/level" (Task 5 code) — fixed inside Task 7's dispatch, with a key-order assertion — spec D5/D6 say first keys in both shapes — if wrong: a cosmetic key-order change in the rarest report.
Task 6: minor (deferred): deep-segment skip-rule/inspection cases of readEntry untested; misleading message for { auto:'random', extra:1 }; vestigial autoAt index; duplicated "usable read" predicate; shallow freeze of the sources list.
Task 6: complete (commits cfe25c5..bec632b, review clean)
Task 7: dispatched, BASE 83169c0
Task 5: fix round 1/5 (3 addressed, 0 open; commits bec632b..83169c0)
Task 5: complete (commits 4bb4cb3..907fd49 + 83169c0, review clean after round 1)
Task 7: implemented 128e6de (concern: 511/512 bytes in the end-to-end floor test — that is the limiter filling the budget, not the minimal report, which measured 452/487); review dispatched; Task 8 dispatched (BASE 128e6de)
Task 7: review: spec ✅, quality approved, 1 Important (plan-mandated code: number/boolean part values bypass the redaction policy, so a transform-only policy scrubbing numeric ids is skipped and the hash becomes a confirmation oracle)
Task 7: Ruling: fix it — every part value, whatever its type, goes through redactor.apply before hashing — spec D6 says values are taken after redaction, and the fingerprint is shown to untrusted readers — if wrong: a transform sees a few more values than before. Fix queued behind Task 8's implementer; minors 1 (thread key 'fingerprint' into nested redaction) and 2 (cyclic nested value test) ride along.
Task 7: minor (deferred): makeFingerprint vs report path can drift when toCorjAsJson mutates the object (doc sentence); aggregate hash input is unbounded (nodes x parts x 16 KiB) and utf8() builds a number[]; entry labels ignore `inspection`; index.ts is 1962 lines (fingerprint glue could move); stage-conditional transforms defeat the stack prefix cut (falls back to the V8 frame rule).
Task 8: implemented 6b16421..c398c2d (consumers PASS); review dispatched. Task 7 fix round 1 dispatched. Note: corj CHANGELOG.md is generated by semantic-release from commit footers; no manual entry.
Task 7: fix commit 4220381; re-review dispatched. Task 9 dispatched (BASE 4220381)
Task 7: fix round 1/5 (4 addressed, 0 open; commits c398c2d..4220381); re-review security answer: no un-rewritten value reaches the hash input
Task 7: minor (deferred): named field parts (message, stack...) are policy-processed under their own key, so a transform gated strictly on key === 'fingerprint' does not see them (they are already redacted in the report); a string part value is uncapped while nested values are capped at 16 KiB.
Task 7: complete (commits 83169c0..128e6de + 4220381, review clean after round 1)
Task 8: review: spec ❌ (2 small), quality needs fixes — Important 1: tests/api.test.ts:183 lost the explicit-undefined options case; Important 2: two README report blocks still show the 10.x default shape.
Task 8: Ruling: both Task 8 fixes are carried by Task 9's implementer, who owns the working tree (item 1 as its own commit `test: restore the explicit-undefined options case`, item 2 inside the README rewrite) — avoids two agents committing in one tree — if wrong: Task 8's fix is reviewed as part of Task 9's diff instead of its own re-review. Minor 1-3 ride along (redundant ...LEGACY spreads; "ids elided" note).
Task 8: minor (deferred): the real-defaults fuzz test does not assert non-root rows lack the two fields (covered by the Task 6/7 suites).
Task 9: implemented 13873f9 + 43bc11e (carries Task 8's two fixes); review dispatched; dev tarball built from 43bc11e
Task 9: review: spec ✅, quality approved, no Critical/Important; 20 sampled README claims TRUE against HEAD; Task 8's two carried fixes verified in the same diff
Task 8: fix round 1/5 (2 addressed via Task 9's implementer, 0 open; commits 13873f9, 43bc11e; reviewed inside the Task 9 review)
Task 8: complete (commits 128e6de..c398c2d + 43bc11e)
Task 9: minor (deferred): schema-v014 positive test does not assert occurrence_id/fingerprint presence; v0.14 schema files are prettier-formatted while v0.13 are not; trimmed reports append the _omitted flags after v/$schema while the minimal report puts them before v (cosmetic); winston example shows report-order keys; agent-harness example does not showcase `context`.
Task 9: complete (commits 4220381..43bc11e, review clean)
Ruling: the final whole-branch review runs on the session model (Fable), adversarial — the owner asked for Opus for execution; the last two releases each had disclosure bugs that only a Fable review found (project memory) — if wrong: one review seat cost more than Opus would have.
Final review: dispatched (Fable, adversarial), merge base 5ad780e, HEAD 43bc11e
Final review (Fable): FIX FIRST. No Critical. F1 Important: `paths` rules and path-aware transforms are not applied to fingerprint part values (value redacted under node.path, not its own path) -> confirmation oracle. F2 Important: hash input unbounded; makeCorj THROWS RangeError on a 129 MB thrown string with default options (10.0 returned a report), ~15 bytes heap per input byte. F3 design: an invalid runtime occurrenceId call argument throws inside the caller's catch block. F4 Minor: { path: ['ids','0'] } slips past the paths rule `$.ids[0]`. F5 Minor: bigint/NaN part values hash as null; { field:'a' } and { path:['a'] } hash differently. F6 Minor: the `onError threw:` console line is unscrubbed.
Ruling: F1, F2 fixed before merge — both change what fp1_ hashes and cannot change after 11.0.0 ships.
Ruling: F3 — an invalid runtime `occurrenceId` or `fingerprint` call VALUE no longer throws: it is recorded as a reporting error (stage 'other', key occurrence_id / fingerprint) and resolution falls through to the sources; a call input of the wrong SHAPE (not an object, unknown key) still throws — a request id with a space must not turn an error report into a second exception inside a catch block; corj's promise is that only configuration throws — if wrong: a caller's typo in an id is reported in the report instead of thrown. application-exception keeps validating its own occurrenceId first (APPEX_INVALID_OCCURRENCE_ID), unchanged since 0.3.
Ruling: F4, F5, F6 fixed in the same wave — F5 affects the fp1 recipe (now or never); F4 and F6 are one-liners on a disclosure path. bigint contributes `<digits>n`, a non-finite number its String form; { path: ['a'] } takes the label `field:a`.
Ruling: promoted deferred Minors: Task 7 "aggregate hash input unbounded / utf8() number[]" and "string part value uncapped" (= F2); Task 6 "deep-segment readEntry skip-rule cases untested" (written with F1/F4). All other deferred Minors stay deferred.
Final review: ONE fix wave dispatched (Opus), BASE 43bc11e
Final review: fix wave landed 43bc11e..fea9095 (7 commits; 1426 tests, 100%, consumers PASS). Implementer notes: non-string occurrenceId is also reported-not-thrown (covers string[] headers); string cap constant not exported (export surface is snapshot-pinned). One scoped re-review dispatched to the same Fable reviewer.
Final review: scoped re-review (Fable): READY TO MERGE at fea9095; F1-F6 ADDRESSED by rerunning the original reproductions; no regressions (22,272 never-throw runs: only the known #217; 6,000 size-fuzz reports: 0 over budget).
Ruling: residual Minors N1-N5 are not fixed before merge (the process allows one fix wave; none is load-bearing; N1 is safe to fix after release because path spelling is not hashed) — filed as a corj GitHub issue — if wrong: N1 lets `{ path: ['map', 0] }` on a plain object dodge the skip rule `$.map.0`, a caller-spelling edge; N4 a 4M-digit bigint part costs 1.7 s.
Task 10: branch pushed; PR https://github.com/dany-fedorov/caught-object-report-json/pull/220 open, CI green (test, consumers 20, consumers 24). `gh pr merge` was DENIED by the Claude Code permission classifier ("Merge Without Review"). Not worked around (a local merge + push to main would bypass the same guardrail). The merge is the owner's; merging runs semantic-release, which computes 11.0.0.
Dev tarball for appex: .superpowers/sdd/tarballs/corj-11-dev-fea9095.tgz
Post-review addition: appex's final review (I1) needs maker.makeFingerprint(caught, { requireStack }) — one small task on the corj branch, with its own review, before the merge.
Task 11 (post-review addition): implemented 23462f1 + 45b73e5 (requireStack; also treats a no-invoke withheld stack as stackless); review dispatched; tarball corj-11-dev-45b73e5.tgz
Task 11: review: spec ✅ on the letter, README ❌; Important x3: requireStack tested the absence of the text fallback, not the presence of frames (a frameless string stack, a redacted stack, a toCorjAsString hook and a recipe without `stack` all still produced text-derived hashes).
Task 11: Ruling: requireStack returns a fingerprint only when `stack` is in the recipe, the root's stack was really read, and the hashed stack has real frame lines (V8 or SpiderMonkey/JSC pattern); stackWithoutHeader's prefix cut counts only on a line boundary — the option exists for untrusted audiences, so it must demand positive evidence — if wrong: more values get no public fingerprint than strictly necessary. Fix round 1 dispatched.
Task 11: fix commit 6f09b78 (1464 tests, 100%, consumers PASS); re-review dispatched; tarball corj-11-dev-6f09b78.tgz
Task 11: fix round 1/5 (3 addressed, 0 open; commit 6f09b78); re-review: approved, no Critical/Important; 0 of 19 ordinary error shapes moved their report fingerprint.
Task 11: Ruling: two of the re-review's Minors are fixed before merge in round 2 — the @-frame pattern accepted `mail@host.example.com:587` (a guessable, frameless stack passed requireStack), and a skipped stack must be excluded structurally, not by the replacement's text — both are one-liners on the disclosure path — if wrong: a rare engine frame format without a path gets no public fingerprint.
Task 11: minor (deferred): a message embedding a fake "\n    at x" line plus a defeated header cut keeps a message tail in a hash that also carries real frames (no guessing oracle); two predicates now exist (hash fallback vs requireStack) by controller ruling.
Task 11: fix round 2/5 (3 addressed; commit e2a7224: @-frame needs a path; skipped/withheld stack excluded structurally; README clause). Round 3 dispatched: the V8 arm accepted prose (" at the gate"), found by the appex re-review (R2).
Task 11: fix round 3/5 (commit 81f5156: V8 frame lines need a location; real runtime stacks asserted) and round 4/5 (commit 87025aa: README names frameless runtime errors; CRLF tolerated). Re-review 2: approved, ready to merge; 33 of 35 real error shapes pass requireStack (the 2 misses are genuinely frameless runtime errors); 0 report fingerprints moved.
Task 11: minor (deferred): caller-assigned frame-shaped text (`err.stack = '    at PIN 4921 (/a.js:1:1)'`) still passes — no text test can separate it; documented.
Task 11: complete (commits fea9095..87025aa). Controller re-ran the gate at 87025aa: 31 suites, 1495 tests, 100%; consumers PASS. Branch pushed; PR #220 updated.
corj follow-up issue: https://github.com/dany-fedorov/caught-object-report-json/issues/221. PR #220 CI green at 87025aa.
